import React from 'react';
import { Text } from 'react-native';
import { Screen } from '@/components/layout/Screen';

export const DeleteModal: React.FC = function () {
    return (
        <Screen>
            <Text>Delete modal</Text>
        </Screen>
    );
}
